{
  "name": "potok-pipedream-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "dev:emulator": "wrangler dev --port 8788 --var FIREBASE_PROJECT_ID:demo-potok --var AUTH_EMULATOR_HOST:127.0.0.1:9099 --var FIRESTORE_EMULATOR_HOST:127.0.0.1:8080 --var TASK_SEALING_SECRET:local-dev-only --var FIREBASE_WEB_API_KEY:demo-key"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "typescript": "^5.8.2",
    "wrangler": "^4.33.1"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.4.0"
  }
}
